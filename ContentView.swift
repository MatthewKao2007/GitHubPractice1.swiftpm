import SwiftUI

struct ContentView: View {
    @State var Answer = 0
    var body: some View {
        VStack {
            Button("Multiply") {
                <#code#>
            }
            Text("\(Answer)")

        }
    }
}
