import SwiftUI

struct ContentView: View {
    @State var Answer = int * num
    var body: some View {
        VStack {
            Button("Multiply") {
                <#code#>
            }
            Text("\(Answer)")

        }
    }
}
